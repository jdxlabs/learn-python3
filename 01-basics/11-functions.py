#!/usr/bin/env python3

# A function is a block of code which only runs when it is called.
# In Python, we do not use curly brackets, we use indentation with tabs or spaces

def func(var1, var2):
    return 'XyeahX'[var1:var2]


print(func(1, 5))
#   yeah

print(func(2, 4))
#   ah
